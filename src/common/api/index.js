import axios from 'axios'
let api = {
  query (opt) {
    // openid = store.state.openid
    // Object.assign(opt, { openid })
    return axios.post(
      // url,
      // qs.stringify(opt)
    )
  }
}
export default api
